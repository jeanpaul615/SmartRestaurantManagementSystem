# Backend API URL
# Usar /api para desarrollo local (con proxy de Vite)
# Usar http://localhost:8000 si NO usas el proxy
VITE_API_BASE_URL=/api

# WebSocket URL (opcional, para tiempo real)
# VITE_WS_URL=ws://localhost:8000
